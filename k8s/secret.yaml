apiVersion: v1
kind: Secret
metadata:
  name: syncpad-secrets
  namespace: syncpad
  labels:
    app.kubernetes.io/name: syncpad
    app.kubernetes.io/component: secrets
type: Opaque
stringData:
  # Replace with strong generated production secrets prior to applying
  SPRING_DATASOURCE_PASSWORD: "REPLACE_WITH_STRONG_DB_PASSWORD"
  POSTGRES_PASSWORD: "REPLACE_WITH_STRONG_DB_PASSWORD"
  JWT_SECRET: "REPLACE_WITH_256BIT_HEX_SECRET_FROM_OPENSSL"
  RABBITMQ_PASSWORD: "REPLACE_WITH_STRONG_STOMP_PASSWORD"
  BACKUP_PASSPHRASE: "REPLACE_WITH_STRONG_AES256_BACKUP_PASSPHRASE"
  GRAFANA_ADMIN_PASSWORD: "REPLACE_WITH_STRONG_GRAFANA_PASSWORD"
